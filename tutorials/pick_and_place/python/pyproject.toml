[project]
name = "pick-and-place-python"
version = "0.1.0"
description = "Python side of the Unity <-> Python wiring for the pick-and-place RL controller"
requires-python = ">=3.10"
dependencies = [
    "ikpy>=4.0.0",
    "numpy",
    "pygame>=2.6.1",
    "torch>=2.12.1",
]
